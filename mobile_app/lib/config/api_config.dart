class ApiConfig {
  static const String baseUrl = 'http://10.0.2.2:4000/api'; // Use 10.0.2.2 for Android emulator to access host localhost
  // static const String baseUrl = 'http://localhost:4000/api'; // Use for iOS simulator or if host is directly accessible
}
